const nomes = ['Caio', "Maria", 'José', 'Matheus', 'Kaio'];

// nomes[nomes.length] = 'Gabriel';
// delete nomes[1];
// nomes[nomes.length + 1] = 'Mais um vazio';
// nomes.unshift('Primeiro nome:'); // isso vai pro PRIMEIRO.
// // retirando o PRIMEIRO ELEMENTO
// nomes.shift();
// nomes.pop();
// nomes.push('Último nome');
console.log(nomes.slice(1,-1));
console.log(nomes);